import { createContext } from "react";

export const GRIContext = createContext() 

export const GRIContextProvider = () => {
    return (
    <GRIContext.Provider>
         
    </GRIContext.Provider>
)}